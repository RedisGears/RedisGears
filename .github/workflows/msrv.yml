name: Pull Request

on:
    push:
        paths-ignore:
            - '.circleci/**'
            - 'docs/**'
            - '*.md'
        branches:
            - main
            - master


jobs:

    jammy-x86:
        uses: ./.github/workflows/REUSABLE_DOCKER_BUILDER.yml
        with:
            os_version: 22.04
            os_family: ubuntu
            dockerfile: dockerbuilds/dockerfile.debian.tmpl
            msrv: 1.65.0
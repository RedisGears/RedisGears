
SRCDIR=../../src/deps/cpython
export CFLAGS=-fPIC -DREDIS_ALLOC -DPy_UNICODE_WIDE

all:
ifeq (,$(wildcard Makefile))
	@$(SRCDIR)/configure --without-pymalloc
endif
	@$(MAKE)

clean:
ifeq ($(ALL),1) 
	find . \( ! -name "Makefile.main" \) -delete
else
	$(MAKE) clean
endif
